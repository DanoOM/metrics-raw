package org.dshops.metrics;

/** A Market interface, if any listener requires The MetricRegistry perform sub-millis indexing, it should must
 * implement this interface. */
public interface EventIndexingListener extends EventListener {

}
